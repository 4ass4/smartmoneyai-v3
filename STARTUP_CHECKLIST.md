# ✅ Чеклист для запуска SmartMoneyAI v3

## 🔑 Необходимые токены и ключи

### 1. Telegram Bot Token
- **Источник**: [@BotFather](https://t.me/BotFather)
- **Действия**:
  1. Откройте @BotFather в Telegram
  2. Отправьте `/newbot`
  3. Следуйте инструкциям
  4. Скопируйте токен (формат: `123456789:ABCdef...`)
- **Переменная**: `TELEGRAM_BOT_TOKEN`

### 2. Telegram Chat ID
- **Источник**: [@userinfobot](https://t.me/userinfobot)
- **Действия**:
  1. Откройте @userinfobot в Telegram
  2. Отправьте любое сообщение
  3. Скопируйте ваш ID (число, например: `123456789`)
- **Переменная**: `TELEGRAM_CHAT_ID`

### 3. BingX API Key
- **Источник**: https://bingx.com/en-us/api
- **Действия**:
  1. Зарегистрируйтесь на BingX
  2. Перейдите в раздел API
  3. Создайте новый API ключ
  4. **ВАЖНО**: Выберите права:
     - ✅ Read Market Data
     - ✅ Orderbook Access
     - ✅ Trades Access
     - ❌ Trading (не требуется)
- **Переменная**: `BINGX_API_KEY`

### 4. BingX Secret Key
- **Источник**: Там же, где API Key
- **Действия**: Скопируйте Secret Key при создании API ключа
- **ВАЖНО**: Сохраните сразу, потом не будет виден!
- **Переменная**: `BINGX_SECRET_KEY`

## 📋 Критически важно для SVD модуля

### Для полноценной работы SVD Engine требуется:

1. **Доступ к стакану заявок (Orderbook)**
   - Метод API: `GET /openApi/swap/v3/market/depth`
   - Используется для анализа поглощений и дисбаланса
   - **Без этого**: SVD будет работать в ограниченном режиме

2. **Доступ к потоку сделок (Trades)**
   - Метод API: `GET /openApi/swap/v3/market/trades`
   - Используется для расчета дельты и агрессии
   - **Без этого**: Анализ намерений крупных игроков недоступен

### ⚠️ Если нет доступа к стакану и сделкам:
- SVD модуль вернет `{"intent": "unclear", "confidence": 0}`
- Анализ поглощений не будет работать
- Confidence Score будет занижен

### Пояснения ключевых факторов
- **Спуф-стенка (bid)** — крупная лимитная заявка на покупку, может быть фейковой, создает видимость поддержки снизу; если исчезает без движения цены — это манипуляция.
- **Фаза: distribution** — по SVD поведение похоже на распределение: крупные игроки выгружают позиции, медвежий контекст, часто перед выходом вниз или после свипа вверх.
- **HTF ликвидность: 1) neutral, 2) neutral** — на старших ТФ (например, 1ч/4ч) нет явного перекоса ликвидности; старший фон не тянет ни вверх, ни вниз, локальные сигналы важнее.

## 🚀 Быстрый старт

### Шаг 1: Установка зависимостей
```bash
pip install -r requirements.txt
```

### Шаг 2: Создание .env файла
```bash
# Windows
copy .env.example .env

# Linux/Mac
cp .env.example .env
```

### Шаг 3: Заполнение .env
Откройте `.env` и заполните:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_CHAT_ID=ваш_chat_id_от_userinfobot
BINGX_API_KEY=ваш_api_ключ_от_BingX
BINGX_SECRET_KEY=ваш_secret_ключ_от_BingX
```

### Шаг 4: Запуск
```bash
python main.py
```

## 🔍 Проверка настройки

### Проверка 1: Telegram бот
```python
# В Python консоли
from telegram import Bot
import asyncio

async def test():
    bot = Bot(token="ваш_токен")
    me = await bot.get_me()
    print(f"Бот работает: {me.username}")

asyncio.run(test())
```

### Проверка 2: BingX API
```python
# В Python консоли
from api.bingx_client import BingXClient

client = BingXClient(
    api_key="ваш_ключ",
    secret_key="ваш_secret"
)

# Проверка OHLCV
klines = client.get_klines("BTC-USDT", "1h", 10)
print(f"OHLCV данные: {klines is not None}")

# Проверка стакана
orderbook = client.get_orderbook("BTC-USDT", 20)
print(f"Стакан доступен: {orderbook is not None}")

# Проверка сделок
trades = client.get_trades("BTC-USDT", 100)
print(f"Сделки доступны: {trades is not None}")
```

## ⚠️ Частые проблемы

### Проблема: "Бот или chat_id не настроены"
**Решение**:
- Проверьте, что `.env` файл существует
- Убедитесь, что переменные загружаются (используется `python-dotenv`)
- Проверьте правильность токена и Chat ID

### Проблема: "Нет данных OHLCV"
**Решение**:
- Проверьте подключение к интернету
- Проверьте правильность API ключей BingX
- Убедитесь, что символ указан правильно (по умолчанию `BTC-USDT`)

### Проблема: "SVD данные недоступны"
**Решение**:
- Проверьте, что API ключ имеет права на чтение стакана и сделок
- Убедитесь, что методы `get_orderbook()` и `get_trades()` возвращают данные
- Без этих данных SVD будет работать в ограниченном режиме

## 📊 Итоговый чеклист

- [ ] Python 3.8+ установлен
- [ ] Все зависимости установлены (`pip install -r requirements.txt`)
- [ ] Telegram бот создан через @BotFather
- [ ] Telegram токен получен и сохранен
- [ ] Chat ID получен через @userinfobot
- [ ] BingX аккаунт создан
- [ ] BingX API ключ создан с правильными правами
- [ ] BingX Secret ключ сохранен
- [ ] Файл `.env` создан из `.env.example`
- [ ] Все переменные в `.env` заполнены
- [ ] Директории `data/`, `logs/` существуют
- [ ] Проверка Telegram бота прошла успешно
- [ ] Проверка BingX API прошла успешно
- [ ] Система запущена (`python main.py`)

## 🎯 После запуска

1. Проверьте логи в `logs/smartmoney.log`
2. Убедитесь, что бот отвечает на команды
3. Дождитесь первого сигнала (может занять несколько минут)
4. Проверьте, что сигналы приходят в Telegram

## 📝 Примечания

- **Без BingX API ключей**: Система не сможет получать данные
- **Без Telegram токена**: Сигналы не будут отправляться
- **Без доступа к стакану/сделкам**: SVD анализ будет ограничен
- **Минимальный интервал анализа**: 60 секунд (настраивается в `config.py`)

