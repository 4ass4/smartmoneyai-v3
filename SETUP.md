# 🚀 Инструкция по настройке SmartMoneyAI v3

## 📋 Требования для запуска

### 1. Python окружение
- Python 3.8 или выше
- pip (менеджер пакетов Python)

### 2. API ключи и токены

#### Telegram Bot
1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
3. Получите ваш Chat ID:
   - Напишите [@userinfobot](https://t.me/userinfobot)
   - Скопируйте ваш ID (число, например: `123456789`)

#### BingX API
1. Зарегистрируйтесь на [BingX](https://bingx.com)
2. Перейдите в раздел API: https://bingx.com/en-us/api
3. Создайте API ключ с правами:
   - ✅ **Чтение данных** (Read-only)
   - ✅ **Доступ к стакану** (Orderbook)
   - ✅ **Доступ к сделкам** (Trades)
   - ❌ Торговля (не требуется)

## 🔧 Установка

### Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 2: Настройка переменных окружения

1. Скопируйте файл `.env.example` в `.env`:
```bash
cp .env.example .env
```

2. Откройте `.env` и заполните:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
BINGX_API_KEY=ваш_api_ключ
BINGX_SECRET_KEY=ваш_secret_ключ
```

### Шаг 3: Проверка структуры проекта

Убедитесь, что все директории созданы:
```
smartmoneyai/
├── data/
│   ├── cache/
│   └── samples/
├── logs/
└── ...
```

## ▶️ Запуск

### Запуск основной системы

```bash
python main.py
```

Система будет:
1. Подключаться к BingX API
2. Получать данные (OHLCV, стакан, сделки)
3. Анализировать рынок через все модули
4. Отправлять сигналы в Telegram при обнаружении

## 📱 Использование Telegram бота

После запуска системы, бот будет автоматически отправлять сигналы в указанный чат.

### Команды бота (если добавить обработчики):
- `/start` - приветствие
- `/status` - статус системы
- `/signal` - последний сигнал
- `/help` - помощь

## ⚠️ Важные замечания

### Для работы SVD модуля требуется:
- ✅ Доступ к **стакану заявок** (Orderbook) через BingX API
- ✅ Доступ к **потоку сделок** (Trades) через BingX API
- ✅ API ключ с правами на чтение рыночных данных

### Без этих данных:
- SVD модуль будет работать в ограниченном режиме
- Анализ поглощений и агрессии будет недоступен
- Confidence Score может быть ниже

## 🔍 Проверка работы

1. Проверьте логи в `logs/smartmoney.log`
2. Убедитесь, что бот отправляет тестовое сообщение
3. Проверьте, что данные приходят с BingX API

## 🐛 Решение проблем

### Ошибка: "Бот или chat_id не настроены"
- Проверьте, что `.env` файл создан и заполнен
- Убедитесь, что переменные окружения загружаются

### Ошибка: "Нет данных OHLCV"
- Проверьте подключение к интернету
- Проверьте правильность API ключей BingX
- Убедитесь, что символ (SYMBOL) указан правильно

### Ошибка: "Ошибка отправки сигнала"
- Проверьте токен бота
- Проверьте Chat ID
- Убедитесь, что бот запущен (напишите ему /start)

## 📞 Поддержка

При возникновении проблем проверьте:
1. Логи в `logs/smartmoney.log`
2. Правильность всех токенов и ключей
3. Доступность API BingX

