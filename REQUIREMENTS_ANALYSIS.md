# 📊 Анализ требований для запуска проекта

## ✅ Что нужно для запуска

### 1. **Telegram Bot Token**
- **Где получить**: [@BotFather](https://t.me/BotFather) в Telegram
- **Формат**: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
- **Назначение**: Отправка сигналов и уведомлений
- **Переменная**: `TELEGRAM_BOT_TOKEN`

### 2. **Telegram Chat ID**
- **Где получить**: [@userinfobot](https://t.me/userinfobot) в Telegram
- **Формат**: Число, например `123456789`
- **Назначение**: ID чата для отправки сигналов
- **Переменная**: `TELEGRAM_CHAT_ID`

### 3. **BingX API Key**
- **Где получить**: https://bingx.com/en-us/api
- **Требуемые права**:
  - ✅ Чтение рыночных данных (Read Market Data)
  - ✅ Доступ к стакану заявок (Orderbook)
  - ✅ Доступ к потоку сделок (Trades)
  - ❌ Торговля (не требуется)
- **Назначение**: Получение OHLCV данных, стакана и сделок
- **Переменная**: `BINGX_API_KEY`

### 4. **BingX Secret Key**
- **Где получить**: Там же, где API Key
- **Назначение**: Подпись запросов к API
- **Переменная**: `BINGX_SECRET_KEY`

## 🔑 Критически важные данные для SVD модуля

### Для работы SVD Engine требуется:

1. **Стакан заявок (Orderbook)**
   - Метод: `get_orderbook()` в `BingXClient`
   - Используется для:
     - Анализа поглощений
     - Определения дисбаланса
     - Поиска крупных лимитных ордеров

2. **Поток сделок (Trades)**
   - Метод: `get_trades()` в `BingXClient`
   - Используется для:
     - Расчета дельты (delta)
     - Определения агрессии покупателей/продавцов
     - Анализа скорости сделок (velocity)

### Без этих данных:
- ⚠️ SVD модуль работает в ограниченном режиме
- ⚠️ Confidence Score может быть занижен
- ⚠️ Анализ намерений крупных игроков недоступен

## 📦 Зависимости проекта

### Основные библиотеки:
- `python-telegram-bot>=20.7` - для работы с Telegram
- `requests>=2.31.0` - для HTTP запросов к BingX API
- `pandas>=2.1.0` - для обработки данных
- `numpy>=1.24.0` - для математических операций
- `python-dotenv>=1.0.0` - для загрузки .env файла

### Установка:
```bash
pip install -r requirements.txt
```

## 🔧 Настройка

### Шаг 1: Создать .env файл
```bash
cp .env.example .env
```

### Шаг 2: Заполнить .env
```env
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_CHAT_ID=ваш_chat_id
BINGX_API_KEY=ваш_api_ключ
BINGX_SECRET_KEY=ваш_secret_ключ
```

### Шаг 3: Запустить
```bash
python main.py
```

## ⚠️ Важные замечания

1. **API ключи BingX должны иметь права на чтение данных**
   - Без этого стакан и сделки будут недоступны
   - SVD анализ не будет работать полноценно

2. **Telegram бот должен быть запущен**
   - Напишите боту `/start` перед запуском системы
   - Убедитесь, что бот активен

3. **Chat ID должен быть правильным**
   - Для личных сообщений: ваш User ID
   - Для группы: ID группы (можно получить через бота)

## 🧪 Проверка настройки

### Проверка Telegram:
```python
# В Python консоли
from telegram import Bot
bot = Bot(token="ваш_токен")
await bot.get_me()  # Должен вернуть информацию о боте
```

### Проверка BingX API:
```python
# В Python консоли
from api.bingx_client import BingXClient
client = BingXClient(api_key="ваш_ключ", secret_key="ваш_secret")
klines = client.get_klines("BTC-USDT", "1h", 10)
print(klines)  # Должен вернуть данные
```

## 📝 Итоговый чеклист

- [ ] Python 3.8+ установлен
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Telegram бот создан через @BotFather
- [ ] Telegram токен получен
- [ ] Chat ID получен через @userinfobot
- [ ] BingX аккаунт создан
- [ ] BingX API ключ создан с правами на чтение
- [ ] BingX Secret ключ получен
- [ ] Файл `.env` создан и заполнен
- [ ] Директории `data/`, `logs/` созданы
- [ ] Система запущена (`python main.py`)

